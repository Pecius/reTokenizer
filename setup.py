#!/usr/bin/env python

from distutils.core import setup

setup(name="reTokenizer",
      version="1.0",
      description="A small regex based tokenizer",
      author="Pecius",
      url="https://github.com/Pecius/reTokenizer",
      packages=["retokenizer"]
      )
